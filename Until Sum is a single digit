#include<stdio.h>
int main()
{
	int n, i=1, sum=0,r;
	scanf("%d", &n);
	while(i<=2)
	{
		sum=0;
		while(n>0)
		{
			r=n%10;
			sum=sum+r;
			n=n/10;
		}
		printf("%d\n", sum);
		n=sum;
		i++;
		
	}
}
